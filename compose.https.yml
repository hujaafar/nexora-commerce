# BUY-01 learning header
# File purpose: Adds the HTTPS ingress override to the base Compose stack.
# Learning focus: Container networks, health-based dependencies, volumes, environment configuration, and overrides.
services:
  caddy:
    image: caddy:2.10-alpine
    environment:
      DOMAIN: ${DOMAIN:-localhost}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      frontend:
        condition: service_started

volumes:
  caddy-data:
  caddy-config:

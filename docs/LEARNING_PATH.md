# Learning path

This file maps the repository's small Git commits to the concept each commit
introduces. Read the history from oldest to newest:

```bash
git log --oneline --reverse
```

Each commit should compile or represent one coherent infrastructure/documentation
step. Use `git show <commit>` to inspect only the code needed for that concept.
